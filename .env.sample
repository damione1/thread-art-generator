# Thread Art Generator Environment Variables

# Server Configuration
ENVIRONMENT=development
HTTP_SERVER_PORT=8080
GRPC_SERVER_PORT=9090
FRONTEND_URL=http://localhost:3000

# Database Settings
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=threadart

# Auth0 Configuration
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_AUDIENCE=https://api.your-domain.com
AUTH0_CLIENT_ID=your-client-id
AUTH0_CLIENT_SECRET=your-client-secret
# For local development with Traefik, add https://tag.local/callback to Auth0 Allowed Callback URLs

# Email Settings
EMAIL_SENDER_NAME=Thread Art Generator
EMAIL_SENDER_ADDRESS=noreply@example.com
EMAIL_SENDER_PASSWORD=email-password
SENDINBLUE_API_KEY=your-sendinblue-api-key

# Admin Settings
ADMIN_EMAIL=admin@example.com

# Storage
GCS_BUCKET_NAME=your-gcs-bucket
